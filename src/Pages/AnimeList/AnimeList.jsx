import React, { useEffect } from "react";

const AnimeList = () => {
  return (
    <div className="">
      {/* {titles.map((el, index) => (
        <TitleCard el={el} key={index} />
      ))} */}
    </div>
  );
};

export default AnimeList;
